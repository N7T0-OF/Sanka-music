name: Build Debug APK

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'

permissions:
  contents: read

jobs:
  build-debug:
    name: Build debug APK
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: 'recursive'

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: "microsoft"
          cache: 'gradle'

      - name: Build debug APK (FOSS, no Sentry)
        run: |
          chmod +x ./gradlew
          echo "" >> ./gradle.properties
          echo "isFullBuild=false" >> ./gradle.properties
          ./gradlew androidApp:assembleDebug

      - name: Find APK
        run: |
          echo "=== Searching for APK files ==="
          find androidApp/build/outputs -name "*.apk" 2>/dev/null || echo "Not found in androidApp"
          find composeApp/build/outputs -name "*.apk" 2>/dev/null || echo "Not found in composeApp"

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: LiquidFlow-debug-apk
          path: androidApp/build/outputs/apk/debug/*.apk
          if-no-files-found: error
